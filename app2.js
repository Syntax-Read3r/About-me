function getCountry() {
    let res = prompt('So what country are you from?');

    if (res !== 'Zimbabwe') {
        return document.write(<p>are not</p>)
    } else {
        return document.write(<p>are</p>);
    }
 }

 